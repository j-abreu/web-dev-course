const [a, b, c, d] = [4, 5, 6, 3]

const sum = a + b + c + d
const subtraction = a - b
const product = b * c
const division = a / c
const modulus = a % 2

console.log(sum, subtraction, product, division, modulus)